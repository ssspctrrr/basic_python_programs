# define numbers array
numbers = []

# loop 2 iterations
for i in range(1, 3):
    # input number and append it to numbers array
    numbers.append(int(input(f"Enter number {i}: ")))

# print list of numbers between first_number and second_number